<ul>
  <% @movies.each do |movie| %>
    <li>

      <%= button_to movie, method: :delete, class: "delete-button" do %>
        <i class="icon-trash"></i>
      <% end %>
      <%= link_to content_tag(:i, '', class: "icon-edit"), edit_movie_path(movie) %>
      
      <%= link_to movie.title, movie %>
  <% end %>
</ul>