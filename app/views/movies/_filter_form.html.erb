<%= form_tag('/filter_movies') do %>
  <%= select_tag :category, options_from_collection_for_select(Category.all, 'id', 'title') %>
  <%= select_tag :rating, options_for_select(1..10) %>
  <%= submit_tag "Filter" %>
<% end %>